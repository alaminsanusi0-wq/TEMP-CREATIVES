# .env.example
# Admin password for project management
ADMIN_PASSWORD=tempcreatives2026

# Path to the data directory (for SQLite and uploads)
# On Render, this should point to your persistent disk mount path (e.g., /var/data)
DATA_DIR=./data
